server:
  port: ${PORT:8080}  # Port dynamique Railway
  forward-headers-strategy: native

spring:
  servlet:
    session:
      cookie:
        same-site: None
        secure: true
  application:
    name: MyIBC


logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    com.app.MyIBC: TRACE

okta:
  oauth2:
    issuer: https://dev-yico8srs45m4a3ec.us.auth0.com/
    client-id: 0U5UeQ6H879uDEiBFQ1uurFmvSsdLaYx
    client-secret: {yourClientSecret}


  #Configuration de la base de donnée
  datasource:
    url: ${JDBC_DATABASE_URL}  # ✅ Utilisation de la variable d’environnement
    username: ${JDBC_DATABASE_USERNAME}  # ✅ Utilisation de la variable d’environnement$
    password: ${JDBC_DATABASE_PASSWORD}  # ✅ Utilisation de la variable d’environnement $
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  frontend-url: https://my-ibc-frontend.vercel.app # 🔁 Mets ton vrai domaine ici
  jwt:
    secret: ${JWT_SECRET}     # ✅ Utilisation de la variable d’environnement
    expiration: 7200000
